# 运行时数据与产物（不入库）
data-service/.venv/
__pycache__/
*.pyc
web/status.json
web/status_history.json
web/.status.lock
web/server.log
web/server.pid
*.pid
data/
node_modules/

# 本机环境变量（可能含真实密码，不入库；模板见各 .env.example）
.env

# 编辑备份与一次性安装包
*.bak*
*.exe

# 本机部署归档产物
config.json.migrated

# 分发压缩包与 bundle（由 git archive / git bundle 生成，不入库）
dist/

# Docker 安装套件一次性大文件（物理存放在 /mnt/d/Desktop/TEMP/dsh-bi-dashboards-assets/，不入库）
DockerDesktopInstaller.exe
