# Docker Desktop + bi-plugin 安装完成指南

## ✅ 已完成

1. **bi-plugin DSH 插件** - 已安装并配置
2. **Docker Desktop** - 已安装 (版本 4.90.0)
3. **中国镜像加速器** - 已配置

---

## ⚠️ 需要手动操作（重要！）

### 第一步：启动 Docker Desktop

1. 按 `Win` 键，搜索 **"Docker Desktop"**
2. 点击启动（第一次启动可能需要等待 1-2 分钟）
3. 等待系统托盘（右下角）出现鲸鱼图标
4. **右键点击鲸鱼图标 → Troubleshoot → Diagnostics** 确认状态正常

### 第二步：配置 WSL2 集成

1. 右键点击系统托盘的鲸鱼图标
2. 点击 **Settings** (齿轮图标)
3. 左侧选择 **General**
4. ✅ 勾选 **"Use WSL 2 based engine"**
5. 点击 **Apply & Restart**
6. 重启后，左侧选择 **Resources → WSL Integration**
7. ✅ 勾选 **"Ubuntu-22.04"**
8. 点击 **Apply & Restart**

### 第三步：验证 Docker

在 Windows PowerShell 中运行：
```powershell
docker ps
docker info | Select-String "Registry Mirrors"
```

应该能看到：
- `docker ps` 返回空列表（正常）
- `Registry Mirrors` 显示配置的镜像地址

### 第四步：配置 bi-plugin 数据源

当你在局域网内连接数据主机 (192.168.120.192) 后：

1. **在数据主机上启动数据服务：**
   ```bash
   cd bi-plugin
   docker compose up -d
   ```

2. **验证数据服务：**
   ```bash
   curl http://localhost:8600/api/meta/tables
   ```

3. **在本机重启 DSH**

4. **验证连接：**
   ```bash
   curl http://192.168.120.192:8600/api/meta/tables
   ```
   应该返回 JSON 格式的表列表

---

## 📋 配置文件位置

| 文件 | 路径 |
|------|------|
| bi-plugin 配置 | `~/.dsh/profiles/web/node_modules/bi-dashboards-host/config.json` |
| DSH 挂载配置 | `~/.dsh/profiles/web/cordis.patch.yml` |
| Docker 镜像配置 | `C:\ProgramData\docker\config\daemon.json` |
| Docker 设置 | `C:\Users\LIN YU JIAN\AppData\Roaming\Docker\settings-store.json` |

---

## 🔧 Docker 镜像加速器

已配置以下国内镜像：
- 中国科学技术大学：https://docker.mirrors.ustc.edu.cn
- 网易：https://hub-mirror.c.163.com
- 百度：https://mirror.baidubce.com

验证命令：
```bash
docker info | grep -A 5 "Registry Mirrors"
```

---

## 🎯 bi-plugin 功能验证清单

完成后按以下顺序验证：

1. ✅ `curl http://192.168.120.192:8600/api/meta/tables` —— 返回 JSON
2. ✅ DSH 设置页出现「无人超市」分区
3. ✅ 侧栏进入「我的看板」：KPI 有数值、图表渲染
4. ✅ 图表筛选功能正常（下单日期筛选）
5. ✅ 浏览器 DevTools Network：`/bi/vendor/echarts.min.js` 200、`/bi/api` 200

---

## 📞 需要帮助？

遇到问题请检查：
1. Docker Desktop 是否正在运行（系统托盘鲸鱼图标）
2. WSL2 集成是否启用
3. 数据主机网络是否连通
4. 数据主机 `docker compose up -d` 是否运行成功
