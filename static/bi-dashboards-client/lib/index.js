/**
* 无人超市 AI BI 插件 — client 包 host 半部（node 侧）。
* 实际浏览器半部通过 package.json 的 dsh.client 声明由 client-modules 扫描加载；
* 此文件仅作为 cordis 组合行的宿主，保证 loader 能解析该包名。
*/
export function apply(ctx) {}
export const inject = []
